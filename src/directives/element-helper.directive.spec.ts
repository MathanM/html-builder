import { ElementHelperDirective } from './element-helper.directive';

describe('ElementHelperDirective', () => {
  it('should create an instance', () => {
    const directive = new ElementHelperDirective();
    expect(directive).toBeTruthy();
  });
});
